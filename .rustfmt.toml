# These rustfmt options are nightly-only. The justfile fmt lanes run on stable
# rustfmt (the nix devshell's toolchain), so they're disabled here. Re-enable
# under `cargo +nightly fmt` if/when they stabilize — tracking issues below.
#
# group_imports = "StdExternalCrate"      # rustfmt#5083
# format_code_in_doc_comments = true      # rustfmt#3348
